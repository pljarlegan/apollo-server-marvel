version: '3'

services:
  server:
    container_name: node
    image: node:10-slim
    restart: always
    volumes:
      - ./:/home/node/app
    command: bash -c "npm start --prefix /home/node/app"
    ports:
      - 4000:4000
    env_file: .env
    stop_grace_period: 1s


